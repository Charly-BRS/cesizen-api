controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

# Route explicite pour le firewall json_login de LexikJWT.
# Symfony 7 n'enregistre plus cette route automatiquement — elle doit être déclarée ici.
api_login:
    path: /api/auth/login
    methods: [POST]
