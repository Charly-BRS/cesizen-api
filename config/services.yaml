# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    # UserSessionStateProcessor : injecte l'utilisateur connecté lors de la création d'une session.
    # Le "innerProcessor" est le processor par défaut d'API Platform (persist en base).
    # On lui passe via le tag "api_platform.state_processor" avec le bon alias.
    App\State\UserSessionStateProcessor:
        arguments:
            $innerProcessor: '@api_platform.doctrine.orm.state.persist_processor'

    # ArticleStateProcessor : injecte l'auteur connecté lors de la création d'un article.
    App\State\ArticleStateProcessor:
        arguments:
            $innerProcessor: '@api_platform.doctrine.orm.state.persist_processor'

    # BreathingExerciseStateProcessor : gère la suppression conditionnelle des exercices.
    # Utilise le remove_processor (DELETE) comme inner processor car il intercepte un DELETE.
    App\State\BreathingExerciseStateProcessor:
        arguments:
            $innerProcessor: '@api_platform.doctrine.orm.state.remove_processor'
